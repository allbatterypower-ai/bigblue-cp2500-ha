{
  "title": "BigBlue CP2500 BLE",
  "config": {
    "step": {
      "user": {
        "title": "Connect BigBlue CP2500",
        "description": "Enter the Bluetooth MAC address of the power station.",
        "data": {
          "address": "Bluetooth MAC address"
        }
      }
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "BigBlue CP2500 options",
        "description": "GitHub snapshot settings. Configure manual upload credentials and optional automatic logging.",
        "data": {
          "github_repo": "GitHub repository (owner/repo)",
          "github_token": "GitHub fine-grained token",
          "auto_log_interval": "Automatic logging interval (minutes; 0 = off)",
          "auto_log_min_soc": "Automatic logging minimum SOC (%)",
          "auto_log_only_ac_connected": "Automatic logging only while AC input is connected"
        }
      }
    }
  }
}
